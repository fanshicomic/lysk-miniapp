<view class="main-content">
  <view class="back-button" bindtap="onBack">
    <svg src="/assets/back.svg" color="white" width="13px" height="13px"></svg>
  </view>
  <view class="content-wrapper">
    <text class="h1">面板分析</text>
    <view class="hr" />
    <view id="panel-inputs" wx:if="{{uploadVisible}}">
      <upload-form id="upload-form" is-analyze="true"></upload-form>
      <view class="button-row display-row">
        <button class="btn ocr-action-btn w-50" bindtap="onOCR">
            识图
        </button>
        <button class="btn submit-btn w-50" bindtap="onSubmit">
            分析
        </button>
      </view>
    </view>
    <view class="hr" />
    <!-- Display analysis results -->
    <view id="analyze-results-wrapper" wx:if="{{analyzeResultsVisible}}">
      <text id="analyze-results-title" class="mt-5 h2">分析结果</text>
      <view id="analyze-results"> 
        面板战力：{{noBuffedCP}}
        加成后战力：{{buffedCP}}
      </view>
    </view>
    <toast id="toast"></toast>
  </view>
  <footer />
</view>
